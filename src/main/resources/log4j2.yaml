Configuration:
  thresholdFilter:
    level: debug
  appenders:
    Console:
      name: STDOUT
      PatternLayout:
        Pattern: "%d{HH:mm:ss.SSS} %-5level %logger{16} - %m%n"

  Loggers:
    Root:
      level: info
      AppenderRef:
        ref: STDOUT